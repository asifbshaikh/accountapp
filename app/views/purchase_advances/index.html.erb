<section id="content">
  <section class="main padder">
    <div class="row">
      <div class="col-lg-4">
        <h2><i class="icon-file text"></i>Payment Vouchers</h2>
      </div>

      <div class="col-lg-8 m-t-large" align="right">
        <%= link_to "<i class='icon-plus'></i> Add New Record".html_safe, new_purchase_advance_path, :class => 'btn btn-success btn-lg' %>
    <a href="#" class="btn btn-white btn-lg " id="use_filter" title='Filter'><i class="icon-filter"></i></a>
    <p></p>
      </div>
    </div>
    <div class="row">
      <div class="col-lg-12">
        <section class="panel">
          <%= render :partial => "shared/flash_msg"%>


              <div class="tab-pane" id="advance-payment">
                <div class='table-responsive'>
                  <table class="table table-striped m-b-none text-small" id="advance_purchase_advances" data-source="<%= purchase_advances_url(:format=>'json', :voucher_type=>'1')%>">
                    <thead>   
                      <%= render :partial => "header" %>
                    </thead>
                    <tbody>
                    </tbody>
                    <tfoot>
                      <%= render :partial => "footer" %>
                    </tfoot>
                  </table>
                </div>
              </div>

            </div>
          </div>
        </section>
      </div>
    </div>
  </section>
</section>