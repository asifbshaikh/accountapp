<div class="btn-group">
  <%= link_to raw('<i class="icon-edit"> </i> Edit'), edit_purchase_return_path(@purchase_return), :class => 'btn btn-info btn-lg' %>
  <button class="btn btn-info btn-lg dropdown-toggle" data-toggle="dropdown">
    <span class="caret"></span><i></i>
  </button>
  <ul class="dropdown-menu">
    <% if @purchase_return.allocation_enable? %>
    <li class="text-left"><%= link_to raw('<i class="icon-anchor"> </i>Allocate Amount'), "/debit_notes/allocate?id=#{@purchase_return.debit_note.id}" %></li>
    <% end %>
    <li class="text-left"><%= link_to raw('<i class="icon-trash"> </i>Delete this purchase'), purchase_return_path(@purchase_return), :method => "delete", :confirm => "Are you sure?" %></li>
  </ul>
</div>