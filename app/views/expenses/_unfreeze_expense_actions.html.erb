<%  if @expense.credit_expense? && !@expense.paid? %>
	<button class="btn btn-success btn-lg dropdown-toggle" data-toggle="modal" data-target='#modal-make-payment'>
		<span class="dropdown-label"><i class="icon-ok-circle"></i> Make Payment</span>  
	</button> 
<% end %>
<%= render "frozen_expense_actions" %>
<%= link_to raw('<i class="icon-print"> </i>'), expense_path(@expense, :format => 'pdf'), :class => 'btn btn-white btn-lg', :target=>"_blank" %>
<%= link_to raw('<i class="icon-mail-reply"> </i>'), expenses_path, :class => 'btn btn-default btn-lg' %>