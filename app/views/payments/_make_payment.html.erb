<tr>
    <td><%= link_to payment.payment_date, payment, :onclick => 'Modalbox.show( this.href , {title: "Profit Next", width:400}); return false;' %></td>
    <td><%= payment.amount %></td>
    <td><%= payment.payment_mode %></td>
    <td><%= Account.find(payment.from_account_id).name %></td>
    <td><%= Account.find(payment.paid_to_account_id).name %></td>
     <td><%= link_to 'Delete', payment, :confirm => 'Are you sure?', :method => :delete,:class => "btn btn-gray" %></td>
  </tr>