<%= form_for(@gst_debit_note) do |f| %>
  <% if @gst_debit_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gst_debit_note.errors.count, "error") %> prohibited this gst_debit_note from being saved:</h2>

      <ul>
      <% @gst_debit_note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
