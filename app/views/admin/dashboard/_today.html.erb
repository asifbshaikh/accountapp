<div class="table-responsive row">
  <table id="today_teams_tbl" class="table table-striped m-b-none text-small" data-source="<%= admin_dashboard_index_url(:format => 'json', :tab_ref=> "todays_leads") %>">
    <thead>
      <%= render :partial=> "admin/dashboard/header"%>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>
