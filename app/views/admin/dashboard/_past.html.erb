            <div class="table-responsive row">
            <table id="past_leads_tbl" class="table table-striped m-b-none text-small" data-source="<%= admin_dashboard_index_url(:format => 'json',:tab_ref=> "past_leads") %>">
              <thead>
                <%= render :partial=> "admin/dashboard/header"%>
              </thead>
              <tbody>
              </tbody>
            </table>
            </div>

