<section id="content">
  <section class="main padder">
    <div class="row">
      <div class="col-lg-6">
        <h2><i class="icon-file-text-alt text"></i> Withdrawal Cash Detail </h2>
      </div>
      <div class="col-lg-6 m-t-large" align="right">
        <a href="/withdrawals/new" class="btn btn-success btn-lg"> + New Transaction</a>
        <div class="btn-group">
      <button  onclick="location.href= '<%= edit_withdrawal_path(@withdrawal)%>'"; class="btn btn-info btn-lg" > <i class="icon-edit"> </i> Edit</button>
          <button class="btn btn-info btn-lg dropdown-toggle" data-toggle="dropdown">
            <span class="caret"></span><i></i>
          </button>
          <ul class="dropdown-menu">
            <li><%= link_to raw('<i class="icon-trash"> </i>Delete '), withdrawal_path(@withdrawal), :method => "delete", :confirm => "Are you sure?" %></li>
          </ul>
        </div>
        <%= link_to raw('<i class="icon-mail-reply"> </i>Back'), banking_index_path, :class => 'btn btn-default btn-lg' %>
        <p></p>
      </div>  
    </div>
    <section class="panel">
      <div class="panel-body">
        <div class="row">
          <!-- Invoice second section -->
          <div class="col-sm-8">
            <section class="panel">
              <div class="panel-body">
                <!-- Invoice basic details -->
                <div class="col-sm-7">
                  <p>
                    <strong> Voucher Number: </strong><%= @withdrawal.voucher_number %>
                  </p>
                  <p><strong> From Account: </strong> <%= @withdrawal.from_account_name %></p>
                  <p><strong> To Account: </strong> <%= @withdrawal.to_account_name %></p>
                  <p><strong> Amount: </strong> <%= format_currency format_amount(@withdrawal.amount) %></p>
                </div>
              </div>
              
              <footer class="panel-footer">
                <div class="row">
                  <div class="col-sm-6"><i class="icon-calendar"> </i> Withdrew on <%=@withdrawal.transaction_date %> by <%= User.find(@withdrawal.created_by).full_name %></div>
                  
                </div>
              </footer>
            </section>
          </div>
          <!-- Invoice second section ends-->
        </div>
        <!-- first row ends -->
        <!-- Invoice table ends -->
      <div class="row">
          <%if !@withdrawal.description.blank? %>
          <div class="col-sm-6">
            <p>
              <label for="productcat">Description</label><br/>
              <%= breaking_word_wrap(@withdrawal.description)%>
            </p>
          </div>
          <% end %>
        </div>
      </div>
    </section>
  </section>
</section>