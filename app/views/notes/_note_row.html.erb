<% @notes.each do |note| %>
<tr class="gradeB">
                      <td class="title" colspan="3">
				           <a href="#" class="nyroModal"><%= link_to truncate(note.subject, :length => 20) ,note_path(note), :onclick => 'Modalbox.show( this.href , {title: "Profit Next", width:400}); return false;' %></a>
			   
			          </td>
				      <td><%= truncate(note.description, :length => 20) %></td>
                      <td><%= note.created_at.strftime("%d-%m-%Y")  %></td>
                      <td><%= note.created_by %></td>
               </tr>
 <% end %>			   