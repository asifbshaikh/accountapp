  <section id="content">
    <section class="main padder">
      <div class="row">
        <div class="col-lg-6">
       <h2><i class="icon-file text"></i>Simple Accounting Entry</h2>
        </div>
        <div class="col-lg-6 m-t-large" align="right">
          <a href="/journals/new" class="btn btn-success btn-lg"><i class='icon-plus'></i> Add New Record</a>
         <a href="#" class="btn btn-white btn-lg" id="use_filter" title="Filter"><i class="icon-filter"></i></a>
          <p></p>
        </div>  
      </div>
      <div class="row">
        <div class="col-lg-12">
          <section class="panel">
          <div class="panel-body">  
          <%= render :partial=> "filter" %>
          <%= render :partial=> "shared/flash_msg"%>
            <div class="table-responsive">
            <div class="alert alert-info">
             <h6><i class="icon-info-sign icon-large"></i> We have merged journal voucher and simple accounting entry voucher. Please use journal voucher to add new record, you can still access your old records from here.</h6>
            </div>
              <table id="saccountings" class="table table-striped m-b-none text-small" data-source ="<%= saccountings_url(:format => "json") %>" >
                <thead>
                  <%= render :partial => "header" %>
                </thead>
                <tbody>
            
                </tbody>
                <tfoot>
                  <%= render :partial => "footer" %>
                </tfoot>
              </table>
            </div>
            </div>
          </section>
        </div>
      </div>
   </section>
  </section>


