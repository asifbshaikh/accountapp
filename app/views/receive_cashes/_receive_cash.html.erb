<tr>
    <td><%= link_to receive_cash.received_date, receive_cash, :onclick => 'Modalbox.show( this.href , {title: "Profit Next", width:400}); return false;' %></td>
    <td><%= receive_cash.amount %></td>
    <td><%= receive_cash.receipt_mode %></td>
    <td><%= Account.find(receive_cash.from_account_id).name %></td>
    <td><%= Account.find(receive_cash.deposit_to_account_id).name %></td>
    <td><%= link_to 'Delete', receive_cash, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>