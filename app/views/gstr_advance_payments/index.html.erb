  <section id="content">
    <section class="main padder">
      <div class="row">
        <div class="col-lg-6">
       <h2><i class="icon-file text"></i>Advance Payments</h2>
        </div>
        <div class="col-lg-6 m-t-large" align="right">
          <a href="/gstr_advance_payments/new" class="btn btn-success btn-lg" onclick="_g"> New Advance Payment</a>
            <a href="#" class="btn btn-white btn-lg " id="use_filter" title='Filter'><i class="icon-filter"></i></a> 
       <!-- a href="#" class="btn btn-default btn-lg"><i class="icon-info"> </i> </a> -->
            <p></p>
        </div>  
      </div>
      <div class="row">
        <div class="col-lg-12">
          <section class="panel">
            <div class="panel-body">
            <%= render :partial=> "filter" %>
            <%= render :partial=> "shared/flash_msg"%>
              <div class="table-responsive row">
                <table id="gstr_advance_payments" class="table table-striped m-b-none text-small" data-source ="<%= gstr_advance_payments_url(:format => "json") %>" >
                  <thead>
                    <%= render :partial => "header" %>
                  </thead>
                  <tbody>
                  </tbody>
                  <tfoot>
                    <%= render :partial => "footer" %>
                  </tfoot>
                </table>
              </div>
            </div>
          </section>
        </div>
      </div>
   </section>
  </section>


<script type="text/javascript">
  var gstr_advance_paymentTable = $('#gstr_advance_payments').dataTable({
      sDom: "<'row'<'col-sm-8'f><'col-sm-4'>r>t<'row'<'col-sm-3'l><'col-sm-4'i><'col col-sm-5'p>>",
      sPaginationType: "full_numbers",
      iDisplayLength:50,
      bFilter: true,
      bProcessing: true,
      bServerSide: true,
      sAjaxSource: $('#gstr_advance_payments').data('source')
      }).columnFilter({aoColumns:[
       { sSelector: "#gstr_advance_paymentno" },
       { sSelector: "#gstr_advance_paymentcustomer", type:"select" },
       { sSelector: "#gstr_advance_paymentDate", type:"date-range"  },
       { sSelector: "#gstr_advance_paymentamount", type: "number-range" },
       { sSelector: "#gstr_advance_paymentstatus", type: "select" }
       ]}
     );

   gstr_advance_paymentTable.fnFilterOnReturn();
</script>