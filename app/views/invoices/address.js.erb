  $(".popup-error").remove();
<% if @invoice.errors.any? %>
  $('.errorSection').after("<%= escape_javascript render('shared/popup_form_error',{:object=>@invoice})%>");
<% else %>
  $("div[id^='model1']").modal('hide');
<% end %>
$(".btnsave").remove();
$(".save-btn").after("<button class='btnsave btn btn-sm btn-success' type='submit'  data-loading-text='Saving...'>Save</button>");
$(".on-go-form").html('<%= escape_javascript render "address_details" %>');