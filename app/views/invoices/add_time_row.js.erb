$("#row-loader").remove();
$("#row_link").before('<%= escape_javascript render( "time_line_items",{ :time_line_item => @time_line_item, :index => params[:index]} ) %>');
$("#row_link").replaceWith('<%= escape_javascript render( "add_time_row_link",{ :index => params[:index].to_i+1} ) %>');
