  $(".alert").remove();
<% if params[:trans_id].blank?%>
  $('.errorSection1').after("<%= escape_javascript render('shared/flash_msg')%>");
  $(".btnsave").remove();
  $(".save-btn").after(" <button  class='btnsave btn btn-sm btn-success' type='submit'  data-loading-text='Saving...''>Submit</button>");
<% else %>
  window.location.reload();
  $(".btnsave").remove();
  $(".save-btn").after("<button class='btnsave btn btn-sm btn-success' type='submit'  data-loading-text='Saving...'>Submit</button>");
  $("div[id^='modal']").modal('hide');
  
  <% end %>