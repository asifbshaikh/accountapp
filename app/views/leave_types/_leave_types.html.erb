<% @leave_types.each do |leave_type| %>
  <tr>
    <td><%= best_in_place leave_type, :leave_type, :path => "/leave_types/#{leave_type.id}", :nil=> '<span class ="variable_item">Click here to add content</span> ', :html_attrs => {:maxlength => 50, :class=>"bg-focus form-control"} %></td>
    <td><%= best_in_place leave_type, :allowed_leaves, :path => "/leave_types/#{leave_type.id}",:html_attrs=>{:maxlength=>40,:class=>"bg-focus  form-control"} %></td>
    <td class="text-center"><a rel="nofollow" data-method="delete" data-confirm="Are you sure?" href= "/leave_types/<%= leave_type.id%>"  title="Delete" ><i class="icon-trash"></i></a></td>
  </tr>
<% end %>