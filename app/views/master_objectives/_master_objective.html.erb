 <tr>
    <td><%= link_to truncate(master_objective.objective_name, :length => 20), master_objective, :onclick => 'Modalbox.show( this.href , {title: "Profit Next", width:400}); return false;' %></td>
    <td><%= truncate(master_objective.details, :length => 20) %></td>
    <td><%= Department.find(master_objective.department_id).name %></td>
    <td><%= link_to 'Delete', master_objective, :confirm => 'Are you sure?', :method => :delete,:class => "btn btn-gray"%></td>
  </tr>