  <section class="panel">
  <header class="panel-heading">
   <strong>Fixed Asset Account Details</strong>
  </header>
  <div class="panel-body">
   <div class="col-sm-6">
    <p><strong>Depreciable:</strong> <div class="btn-group m-t-n-mini m-r-n-mini" data-toggle="button">
                      <% if @account.accountable.depreciable? %>
                        <label class="btn btn-sm btn-white btn-on active">Yes</label>
                        <label class="btn btn-sm btn-white">No</label>
                      <% else %>
                        <label class="btn btn-sm btn-white">Yes</label>
                        <label class="btn btn-sm btn-white btn-on active">No</label>
                      <% end %>  
                      </div>
 </p>
 <p><strong>Depreciable:</strong> <%=@account.accountable.depreciation_rate%></p>
   </div>
  </div>
 </section>

